# Agriculture is rebuilt fresh, not preserved from the live site

Agriculture is rebuilt completely fresh, like every other category, from the master document and this architecture. The master document's "out of scope" note — that Agriculture's existing live pattern-code structure should not be changed — is superseded: the existing live Agriculture page is treated as neither a design nor an implementation constraint. Recording this because the master document states the opposite, and a future reader reconciling the two would otherwise be confused.
