import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Catalogue" };

export default function CataloguePage() {
  return (
    <PagePlaceholder
      eyebrow="Catalogue"
      title="Catalogue"
      description="The responsive catalogue slider and PDF download live here."
      ticket="Ticket 8 (#18)"
    />
  );
}
