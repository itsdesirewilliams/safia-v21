import { PagePlaceholder } from "@/components/page-placeholder";

export const metadata = { title: "Quality First" };

export default function QualityFirstPage() {
  return (
    <PagePlaceholder
      eyebrow="Quality First"
      title="Quality First"
      description="The story rail, testing explanation and machine images live here."
      ticket="Ticket 5 (#15)"
    />
  );
}
